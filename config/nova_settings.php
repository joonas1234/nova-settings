<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Available Settings
    |--------------------------------------------------------------------------
    |
    | Define all settings that are available for editing
    | Only settings that are defined in the list are editable in Nova
    | If list is empty, then all settings in database are editable
    |
    */
    'available_settings' => [
        'foo'
    ]
];